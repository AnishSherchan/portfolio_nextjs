import React from "react";

const Loading = () => {
  return (
    <div className="fixed top-0 left-0 w-full h-full flex items-center justify-center bg-dark_primary_background z-50 text-dark_heading">
      Loading data hello
    </div>
  );
};

export default Loading;
